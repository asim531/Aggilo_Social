---
task: B14
title: "API routes: soul_manifestation_profile and persona_override CRUD"
owners: [backend-eng]
depends_on: [B13, B10]
estimate_hours: 5
status: pending
---

Goal
- Expose endpoints for profile viewing, override management, and audit.

Scope
- Backend API routes only.

Requirements
1. `GET /api/clusters/:id/soul-manifestation` — member-visible summary
2. `GET /api/clusters/:id/persona-override` — view active override
3. `POST /api/admin/clusters/:id/persona-override` — create override (admin only)
4. `PUT /api/admin/clusters/:id/persona-override/:id/approve` — approve override
5. `GET /api/admin/clusters/:id/soul-manifestation/audit` — audit trail
6. `POST /api/admin/clusters/:id/soul-manifestation/regenerate` — trigger Genesis Engine to regenerate profile

Dependencies
- B13 (migration 022)
- B10 (cluster creation API constraints)

Plan
1. Implement route handlers under `apps/api/src/routes/clusters/soul-manifestation.ts`
2. Implement admin route handlers under `apps/api/src/routes/admin/persona-overrides.ts`
3. Add RLS enforcement middleware
4. Add validation using isValidSoulManifestationProfile()
5. Add unit tests for each endpoint

Files to Touch
- `apps/api/src/routes/clusters/soul-manifestation.ts`
- `apps/api/src/routes/admin/persona-overrides.ts`
- `apps/api/test/routes/soul-manifestation.test.ts`

Validation
- Unit tests for each endpoint
- Manual: create override → approve → verify Clio prompt includes override phrases

Risks & Mitigations
- Risk: unauthorized access. Mitigate with admin-only middleware on admin routes.
- Risk: invalid profile submission. Mitigate with validation function.

Out of Scope
- Frontend components; Genesis Engine logic; prompt builder changes.
