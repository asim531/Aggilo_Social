---
task: FE04
title: Activity tab UI (6.1)
owners: [frontend-eng]
depends_on: [B03]
estimate_hours: 5
status: pending
---

Goal
- Implement Activity tab UI with grouped sections, row types, swipe-to-dismiss, and locked absence for Stage 1.

Scope
- Frontend Activity screen and row components.

Requirements
- Stage 1: Activity tab absent from bottom nav (no locked icon).
- Stage 2+: Activity screen: title bar + bell, grouped sections (Today/Yesterday/This Week).
- Row types: Cluster activity (new posts), Clio match, Member joined, Timeline update, DM request.
- Swipe left to Dismiss (client-side state); empty state illustration and copy.
- Accessibility: role/status, aria labels for row actions.

Dependencies
- Activity API (B03) for data; stage flag from profile.

Plan
- Build Activity screen component with grouping by date.
- Implement row components per type with navigation handlers (stubs ok).
- Add swipe-to-dismiss behavior storing dismissed IDs in local state (persist optional).
- Add empty state and handle absence for Stage 1.
- Write UI tests for grouping, dismiss, empty state, stage gating.

Files to Touch
- `src/screens/Activity.tsx`
- `src/components/activity/ActivityRow.tsx`
- Tests under `src/screens/__tests__/Activity.test.tsx`

Validation
- Activity tab hidden for Stage 1 profile; visible for Stage 2+.
- Grouping renders by date; swipe dismiss hides row; empty state shows when none.

Risks & Mitigations
- Risk: date grouping edge cases. Use server timestamps normalized to local.
- Risk: dismiss persistence. Start client-side; add persistence later.

Out of Scope
- Push notifications logic; backend data correctness.
