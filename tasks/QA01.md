---
task: QA01
title: API contract tests (auth, clusters, DM, activity, notifications)
owners: [qa-eng]
depends_on: [B02, B03, B04, B10]
estimate_hours: 6
status: pending
---

Goal
- Add API contract tests for core endpoints to prevent regressions.

Scope
- Test suite additions; no prod code changes except fixtures.

Requirements
- Cover endpoints: auth send/verify OTP; cluster create (with constraints), join; DM request/respond/send; activity list; notification register/update.
- Include success + failure cases (duplicate cluster, founder exclusion, unauthorized DM, Stage gating, empty activity for Stage 1).
- Tests run in CI.

Dependencies
- Implemented APIs from dependent tasks; test framework setup.

Plan
- Add fixtures for users, clusters, tokens.
- Write tests per endpoint with happy-path and negative cases.
- Integrate into CI script.

Files to Touch
- `apps/api/test/*.test.ts` (new/updated)
- CI config if needed

Validation
- CI passes; failing contract breaks pipeline.

Risks & Mitigations
- Risk: flaky OTP. Stub OTP provider.

Out of Scope
- Frontend e2e; LLM calls.
