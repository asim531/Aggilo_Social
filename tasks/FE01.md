---
task: FE01
title: Clio FAB dual-tab UI (cluster/private) with TTL
owners: [frontend-eng]
depends_on: []
estimate_hours: 6
status: pending
---

Goal
- Implement Clio FAB panel with two tabs (Clio, Private 🔒) per spec; Private tab stored in sessionStorage with 12h TTL and countdown.

Scope
- Frontend components/state only; backend untouched.

Requirements
- FAB position per context: cluster screens top-right (40px circle, 16px from right, 8px below top bar); elsewhere bottom-right standard.
- Panel header shows tab labels with underline indicator (no pills). Private header reads “Clio · Private” + countdown “Clears in Xh Xm”.
- Tab switching preserves histories separately; switching does not clear other tab.
- Private tab storage: sessionStorage keyed by session ID; TTL 12h; countdown shown; auto-clear after TTL.
- Empty state Private tab: quiet placeholder only (no onboarding speech).

Dependencies
- None (uses existing FAB shell if present).

Plan
- Build FAB panel component with tab state and content slots.
- Implement storage service for Private tab with TTL + countdown hook.
- Wire positioning variants (cluster vs non-cluster) via props/context.
- Add unit/UI tests for tab persistence and TTL expiry.

Files to Touch
- `src/components/ClioFab.tsx` (and related panel files)
- `src/hooks/useSessionStorageTTL.ts` (new)
- Tests under `src/components/__tests__/ClioFab.test.tsx`

Validation
- Open FAB → switch tabs → content preserved; reload page within TTL keeps Private tab; after TTL countdown ends, history clears.
- Cluster view: FAB top-right; non-cluster: bottom-right.

Risks & Mitigations
- Risk: session ID source. Use existing auth/session context.
- Risk: layout clashes on cluster header. Validate spacing against design tokens.

Out of Scope
- Backend persistence; Clio responses themselves.
