---
task: A10
title: "Observer Dimension 6: Manifestation Alignment + drift detection"
owners: [backend-eng]
depends_on: [B09, A08]
estimate_hours: 7
status: pending
---

Goal
- Observer evaluates whether cluster behaviour matches its configured `soul_manifestation_profile`.

Scope
- Observer introspection engine expansion.

Requirements
- Extend introspection prompt: add Dimension 6 — "Manifestation Alignment"
- Detect drift signals: `silence_expectation: "high"` but agent posts frequently → drift
- Detect register mismatch: `primary_register: "inquiry"` but agent uses declarative statements → drift
- Propose Tier 1 updates: `soul_manifestation_shift` (e.g., reduce `interjection_frequency` if silence expectation violated)
- Add `manifestation_drift_score` to Priority Queue Engine (0–20 points)

Dependencies
- B09 (Observer stewardship tables)
- A08 (soul manifestation generation)

Plan
1. Extend introspection prompt with Dimension 6
2. Implement drift detection heuristics:
   - Post frequency vs silence_expectation
   - Agent statement types vs primary_register
   - Member feedback on "tone feels off"
3. Add `manifestation_drift_score` to priority scoring
4. Implement Tier 1 action: `soul_manifestation_shift`
5. Add unit tests for drift detection

Files to Touch
- `apps/api/src/workers/observer-introspection.ts`
- `apps/api/src/workers/observer-prompt-steward.ts`
- `apps/api/test/observer-introspection.test.ts`

Validation
- Test: cluster with `silence_expectation: "high"` and 5 agent posts/day → drift detected, proposal to reduce cadence
- Test: cluster with `scripture_usage: "none"` but agent cites Quran → drift detected, proposal to suppress scripture

Risks & Mitigations
- Risk: false positive drift. Mitigate with evidence threshold (3+ signals in 7 days).
- Risk: drift detection computationally expensive. Pool B budget covers prompt refinement introspection.

Out of Scope
- Admin UI for drift review; Genesis Engine profile generation.
