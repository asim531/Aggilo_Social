---
task: FE06
title: Messages inbox overlay (4.3)
owners: [frontend-eng]
depends_on: [FE05, B02]
estimate_hours: 5
status: pending
---

Goal
- Build Messages inbox overlay for cluster context with pending requests section and unread indicators.

Scope
- Frontend overlay; uses DM list endpoint.

Requirements
- Entry: Messages icon in cluster top bar (Stage 3+).
- Sections: Pending Requests (amber accent) and Conversations list.
- Each thread row: avatar, nickname, last message preview, timestamp, unread dot if unread.
- Cross-cluster rule: one thread per user-pair; show origin cluster pill in header.
- Empty state: illustration + guidance.

Dependencies
- DM APIs (B02); stage flag; member profile data for avatars.

Plan
- Create overlay screen sliding from right.
- Implement pending requests list with accept/decline actions (hooking to API).
- Implement conversations list with unread state.
- Add tests for rendering sections, empty state, accept/decline flow.

Files to Touch
- `src/screens/ClusterMessages.tsx`
- `src/components/messages/*`
- Tests under `src/screens/__tests__/ClusterMessages.test.tsx`

Validation
- Stage 3 shows icon and overlay; pending requests render; accept/decline updates UI; unread dots display.

Risks & Mitigations
- Risk: unread tracking. Start with simple last_read timestamps; refine later.

Out of Scope
- Realtime updates; DM thread view (already covered by DM feature).
