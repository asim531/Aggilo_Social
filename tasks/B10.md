---
task: B10
title: Cluster creation API constraints (duplicate detection, self-inclusion, score)
owners: [backend-eng]
depends_on: [F03]
estimate_hours: 6
status: pending
---

Goal
- Align cluster creation API with Clio conversational flow: prevent duplicates, enforce founder self-inclusion, compute cluster_score.

Scope
- API handler + services + tests.

Requirements
- Duplicate detection: semantic search against existing clusters (name+purpose+tags); if similar above threshold, return list for FE 5.2.
- Founder self-inclusion: founder gender preselected and non-removable; age range must include founder birth year (snap-back/validation error).
- Cluster score (U-shaped) computation: purpose clarity (30), intentionality (25), name/description quality (20), Clio confidence (25); store in clusters.score.
- Insert cluster + founder auto-join; initialize arc_phase A.

Dependencies
- Prompt builder ceilings (F03) for any LLM scoring helper if used; existing clusters schema; AGGIL Engine.

Plan
- Add duplicate detection service (embedding or trigram) with configurable threshold.
- Extend create endpoint to validate gender/age inclusion; return error payload for FE.
- Add cluster_score computation function; persist to DB.
- Add tests covering duplicate hit/miss, validation failures, score presence.

Files to Touch
- `apps/api/src/routes/clusters/create.ts`
- `apps/api/src/services/cluster-create.ts`
- Tests under `apps/api/test/cluster-create.test.ts`

Validation
- Unit/integration tests pass for duplicate, inclusion checks, score.
- Manual: create cluster with founder exclusion → 400 with message; create near-duplicate → similar list returned.

Risks & Mitigations
- Risk: semantic search infra missing. Mitigate with trigram fallback and TODO to upgrade.
- Risk: performance on large dataset. Add limit and threshold configs.

Out of Scope
- UI; post-creation Atlas/Sage triggers.
