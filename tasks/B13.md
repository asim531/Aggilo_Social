---
task: B13
title: "Migration 022: soul_manifestation_profile fields and override tables"
owners: [backend-eng]
depends_on: [B08]
estimate_hours: 3
status: pending
---

Goal
- Execute Migration 022 with all schema additions for soul manifestation and persona overrides.

Scope
- Single SQL migration file and TypeScript types.

Requirements
- ALTER TABLE for: sage_personas, clio_conversations, cluster_config, observer_prompt_updates
- CREATE TABLE for: cluster_persona_overrides, soul_manifestation_audit
- RLS policies for new tables
- validate_soul_manifestation_profile() SQL function
- TypeScript types at packages/supabase/types/database.ts

Dependencies
- B08 (Genesis Engine schemas)

Plan
1. Write `packages/supabase/migrations/022_soul_manifestation.sql`
2. Update `packages/supabase/types/database.ts` with new table types and enums
3. Verify migration ordering (after 021_observer_stewardship.sql)
4. Run migration locally

Files to Touch
- `packages/supabase/migrations/022_soul_manifestation.sql`
- `packages/supabase/types/database.ts`

Validation
- `supabase db push` succeeds
- `SELECT tablename FROM pg_tables WHERE schemaname='public'` → new tables present
- `\d cluster_config` → new columns present
- TypeScript types compile without errors

Risks & Mitigations
- Risk: migration ordering conflict. Verify 021 applied before 022.
- Risk: existing data incompatible with new constraints. All new columns have DEFAULT NULL.

Out of Scope
- Application code; Genesis Engine logic.
