---
task: FE02
title: Clio AMA Panel UI (3.3) with calibration summary
owners: [frontend-eng]
depends_on: [FE01]
estimate_hours: 6
status: pending
---

Goal
- Implement Clio AMA (Discovery Calibration) panel per screen 3.3 with summary card and adjust interactions.

Scope
- Frontend UI/flows; backend stub ok.

Requirements
- Panel opens from FAB/Tune: header shows Clio avatar + "Help Clio find better" + teal badge "⚙ Discovery Calibration".
- Conversation: initial Clio question pre-rendered, optional 1 follow-up, confirmation with inline calibration summary card (fields: Looking for, Area, Who, Age, Topics, Mode).
- Buttons: “Looks right — show me” (primary) and “Adjust something” (outlined) that makes fields tappable (uses existing settings panel visual).
- On confirm: panel closes; Explore reloads with calibrated params; Tune shows active dot.
- Zero/ambiguous handling per prompt: if ambiguous, follow-up; if user mentions new interest, note it.

Dependencies
- FE01 (FAB shell); backend search for calibrated results (optional stub).

Plan
- Build AMA panel component with chat flow states and summary card.
- Implement adjustable fields (click to open settings panel overlay placeholder or existing component).
- Wire confirm to emit calibration params event for Explore.
- Add UI tests for flow, active dot toggle, summary card fields.

Files to Touch
- `src/components/ClioAmaPanel.tsx` (new)
- `src/hooks/useClioAmaFlow.ts`
- Tests under `src/components/__tests__/ClioAmaPanel.test.tsx`

Validation
- Run through flow: open → answer → summary → confirm → Explore reload event fires; active dot visible.
- Ambiguous reply triggers follow-up; adjust path opens settings overlay.

Risks & Mitigations
- Risk: state explosion. Use finite-state machine for steps.
- Risk: active dot race. Drive from panel state stored in context.

Out of Scope
- Backend calibration logic; Explore search filtering.
