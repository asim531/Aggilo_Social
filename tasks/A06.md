---
task: A06
title: Observer Domain 10 tool analysis trigger + CliToolProposalJob
towners: [backend-eng]
depends_on: [B07, B09]
estimate_hours: 5
status: pending
---

Goal
- Implement Observer Domain 10 flow to trigger Clio tool analysis and draft tool proposals.

Scope
- Observer worker logic, job trigger, proposal draft write.

Requirements
- Read tool gap signals (e.g., high Atlas synthesis rate, Clio zero-match, new cluster tools needing promotion) from findings or scoring.
- On trigger and admin approval (or config), enqueue CliToolProposalJob or directly draft markdown to maintenance/YYYY-MM/.
- proposal markdown uses template at maintenance/templates/TOOL_PROPOSAL_TEMPLATE.md.
- Record tool_proposals row with status pending; admin gate required before activation.

Dependencies
- platform_tools/cluster_tool_enablements schemas (B07); stewardship tables (B09); maintenance template.

Plan
- Extend ObserverDomain10 handler to evaluate triggers and enqueue/draft proposals.
- Implement job processor CliToolProposalJob to write markdown and DB row.
- Add tests: trigger → proposal row + file path; admin gate respected.

Files to Touch
- `apps/api/src/workers/observer-domain10.ts` (or equivalent)
- `apps/api/src/jobs/CliToolProposalJob.ts`
- Tests under `apps/api/test/observer-domain10.test.ts`

Validation
- Unit tests simulate trigger conditions → proposal file path generated and row inserted.
- Ensure no activation without admin status change.

Risks & Mitigations
- Risk: file system path issues. Use maintenance/ path helper and ensure monthly folder exists.
- Risk: over-triggering. Add thresholds and debounce per finding.

Out of Scope
- Tool activation; Sage/Scout tool analysis jobs.
