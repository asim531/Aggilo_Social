---
task: F01
title: Create runtime_events migration
owners: [backend-eng]
depends_on: []
estimate_hours: 3
status: pending
---

Goal
- Add runtime_events table per AGENT_RUNTIME for dispatch/complete/fail/timed_out/rejected logging.

Scope
- Database migration and minimal seeding. No app code changes beyond migration registration.

Requirements
- Table columns exactly per spec: id (uuid), job_id (uuid), job_type (varchar64), agent (varchar32), cluster_id (text nullable), lane (varchar8), status (enum or varchar16), rejection_reason (varchar64 nullable), idempotency_key (varchar256), retry_count (smallint), duration_ms (int), payload_size_bytes (int), created_at (timestamptz default now()).
- Indexes: job_type+created_at desc; status+created_at desc partial for failed/timed_out/rejected.
- Migration idempotent and reversible.

Dependencies
- None.

Plan
- Add SQL migration defining runtime_events table with indexes.
- Register migration in tooling (e.g., Supabase/migrations or db folder).
- Run migration locally to verify.

Files to Touch
- `supabase/migrations/*` (or project migration folder)

Validation
- Run migration: `pnpm supabase db reset` (or project command) succeeds.
- Manual query: insert a test row; select by indexes returns.

Risks & Mitigations
- Risk: table path differs. Mitigate by matching repo migration structure before adding.
- Risk: enum vs varchar mismatch. Use varchar to avoid blocking unless enums pre-defined.

Out of Scope
- Application logging wiring; Observer reading logic.
