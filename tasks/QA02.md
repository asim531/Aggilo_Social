---
task: QA02
title: LLM regression harness (Clio/Sage) with validators
towners: [qa-eng]
depends_on: [F03, F04]
estimate_hours: 6
status: pending
---

Goal
- Add regression harness for Clio/Sage LLM outputs with golden responses and format validators.

Scope
- Test harness + fixtures; no prod prompt changes.

Requirements
- Golden test cases for key op keys (e.g., clio_chat, sage_post_anchor, sage_mention_reply).
- Validators enforce response shape (JSON fields, max length, no forbidden phrases where applicable).
- Snapshot/golden comparison with allowlisted variance; failures break CI.
- Include test for retry+degrade path via mock validator failure.

Dependencies
- Prompt builder ceilings (F03); retry+degrade (F04); op-key registry (F02).

Plan
- Create harness to run LLM calls against mock providers or frozen fixtures.
- Add validators per op key; add golden fixtures.
- Wire to CI target (fast, deterministic).

Files to Touch
- `apps/api/test/llm-regression/*.test.ts`
- Fixture files under `apps/api/test/fixtures/llm/`

Validation
- CI run executes harness; validator or golden mismatch fails.

Risks & Mitigations
- Risk: non-determinism. Use mocked providers or strict temperature=0 for tests.
- Risk: fixture drift. Document update process and require review.

Out of Scope
- Live model calls; full prompt redesign.
