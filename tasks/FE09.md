---
task: FE09
title: Calibrated Explore (3.4) UI
owners: [frontend-eng]
depends_on: [FE02]
estimate_hours: 4
status: pending
---

Goal
- Implement Explore calibrated state UI per screen 3.4.

Scope
- Frontend Explore screen adjustments after AMA calibration.

Requirements
- Tune icon shows active teal dot when calibration applied; reopen shows summary with change/start-fresh options.
- Clio insight pill reflects mode:
  - Relevance: “Showing clusters that specifically match what you described.”
  - Variety: “Opening up the range — here is what is active in your broader area.”
  - Balanced: “Mixing close matches with a few wider options.”
- Calibrated zero state: Clio Prominent + message + buttons (“Adjust settings”, “Keep my settings but show broader matches”).
- Cluster cards render same as 3.2 but filtered per calibrated params.

Dependencies
- FE02 AMA panel emits calibrated params; search API supports params (or stub).

Plan
- Add calibrated state to Explore context; set from AMA confirm.
- Render active dot and calibrated pill text based on mode.
- Implement zero state actions: reopen AMA; broaden search (increment radius) if available.
- Tests for active dot, pill text per mode, zero-state actions.

Files to Touch
- `src/screens/Explore.tsx`
- `src/components/ExploreHeader.tsx`
- `src/components/ClioInsightPill.tsx`
- Tests under `src/screens/__tests__/Explore.test.tsx`

Validation
- After AMA confirm, dot visible; pill text matches mode; zero-state renders buttons.

Risks & Mitigations
- Risk: stale calibration. Clear on session end.

Out of Scope
- Backend search logic.
