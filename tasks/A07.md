---
task: A07
title: Feature Signal capture (Clio) + Observer Domain 11 review
towners: [backend-eng]
depends_on: [B06]
estimate_hours: 5
status: pending
---

Goal
- Capture organic feature signals from Clio conversations, dedupe, and run Observer Domain 11 review.

Scope
- Clio capture logic, storage writes, Observer review job.

Requirements
- Capture only unsolicited member needs; store in feature_signals with feature_hash, scope (cluster/cross-cluster), frequency_count; exclude individual PII in surfaced data.
- Deduplicate by normalized/stemmed hash; increment frequency_count if match.
- Observer Domain 11 job runs monthly to review captured signals: rule compliance, safety, protocol disclosure, k-anonymity (hold until 8 members for cluster scope).
- Status fields: captured → observer_reviewed; reviewer notes.

Dependencies
- feature_signals schema/API (B06); Clio message pipeline; Observer job scheduler.

Plan
- Add Clio pipeline hook to detect qualifying signals and write to feature_signals.
- Implement dedup logic by hash + scope.
- Implement ObserverDomain11 job to review and update status per rules; log findings.
- Add tests: capture/dedupe, non-capture when solicited, review transitions.

Files to Touch
- `apps/api/src/services/clio/feature-signals.ts`
- `apps/api/src/workers/observer-domain11.ts`
- Tests under `apps/api/test/feature-signals.test.ts`

Validation
- Tests cover capture + dedupe; review job updates status; k-anonymity hold enforced.

Risks & Mitigations
- Risk: false positives. Keep detection conservative; allow config.
- Risk: PII leakage. Strip user_id from surfaced data; store only aggregated.

Out of Scope
- UI surfacing; admin dashboard display (handled later).
