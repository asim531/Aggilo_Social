---
task: FE05
title: Members tab action sheet with stage gating + DM request modal (4.2/4.2a)
owners: [frontend-eng]
depends_on: [B01, B02]
estimate_hours: 5
status: pending
---

Goal
- Implement Members tab interactions per spec: stage-based action sheet, DM request modal.

Scope
- Frontend components for 4.2/4.2a; consumes DM APIs.

Requirements
- Action sheet options:
  - Stage 1/2: only “View Profile”.
  - Stage 3: “View Profile” + “Message” → opens DM request modal.
- DM request modal: intro text (150 chars), send, pending state changes button label to “Request Pending”.
- Member rows show badges (Founder), active status dot, shared interests.
- Accessibility: focus trap in modal, aria labels.

Dependencies
- DM APIs (B02); stage flag from profile/context.

Plan
- Add member row component with long-press/tap opening action sheet.
- Implement action sheet with stage gating.
- Implement DM request modal with form and pending state.
- Tests for stage gating, modal submission, pending label.

Files to Touch
- `src/screens/ClusterMembers.tsx`
- `src/components/MembersActionSheet.tsx`
- `src/components/DmRequestModal.tsx`
- Tests under `src/screens/__tests__/ClusterMembers.test.tsx`

Validation
- Stage 1/2 hides Message; Stage 3 shows and opens modal; after send, button shows pending.

Risks & Mitigations
- Risk: stage flag not loaded. Default to safest (hide Message) until flag known.

Out of Scope
- Backend DM logic; realtime updates.
