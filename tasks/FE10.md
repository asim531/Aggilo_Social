---
task: FE10
title: "Cluster info: soul_manifestation_profile summary display"
owners: [frontend-eng]
depends_on: [FE07, AD03]
estimate_hours: 4
status: pending
---

Goal
- Members can see a human-readable summary of how agents show up in this cluster.

Scope
- Frontend React components only.

Requirements
1. In Cluster Info sheet, add a small card: "How our agents show up here"
2. Display: primary register (e.g., "Warmth — our agents hold space gently")
3. If override active, show subtle indicator: "Custom voice calibrated for this room"
4. Voice Preview modal: member taps card → sees sample Clio greeting and Sage post (same as admin preview but read-only)

Dependencies
- FE07 (Cluster Info sheet)
- AD03 (Voice Preview panel)
- B14 (API routes)

Plan
1. Implement SoulManifestationCard component
2. Add to ClusterInfoSheet layout
3. Implement read-only VoicePreviewModal
4. Wire to B14 GET /api/clusters/:id/soul-manifestation endpoint
5. Add tests for rendering

Files to Touch
- `apps/web/src/components/cluster/ClusterInfoSheet.tsx`
- `apps/web/src/components/cluster/SoulManifestationCard.tsx`
- `apps/web/src/components/cluster/VoicePreviewModal.tsx`

Validation
- Manual: open cluster info → see card → tap → preview modal opens
- Test: profile absent → card hidden or shows default message

Risks & Mitigations
- Risk: member confusion about "agents." Use plain language: "How our helper responds here."

Out of Scope
- Admin editing (in AD03); backend API implementation.
