---
task: FE07
title: Cluster Info sheet + milestone modal + share sheet (4.5/4.6)
owners: [frontend-eng]
depends_on: []
estimate_hours: 5
status: pending
---

Goal
- Implement Cluster Info bottom sheet, 10-member milestone modal, and Share sheet per spec.

Scope
- Frontend UI components.

Requirements
- Cluster Info sheet (80% height): cover image, name, created by, AGGIL settings (read-only), Precision Score badge visible to founder only, action list (Edit if members<=1, Notification toggles, Share, Leave, Report).
- 10-member milestone modal (founder-only): blocking modal with Clio, two CTAs, Done link, shown once on threshold.
- Share sheet: mini-preview, share targets row, copy link, note about AGGIL eligibility.
- Accessibility: focus management, aria labels.

Dependencies
- Cluster data incl. score and member count; user role (founder vs member).

Plan
- Build ClusterInfoSheet component with conditional founder-only elements.
- Implement milestone modal trigger when member count crosses 10.
- Implement Share sheet with copy and external share hooks.
- Add tests for founder visibility, milestone trigger, share copy.

Files to Touch
- `src/components/ClusterInfoSheet.tsx`
- `src/components/ClusterMilestoneModal.tsx`
- `src/components/ShareClusterSheet.tsx`
- Tests under `src/components/__tests__/ClusterInfoSheet.test.tsx`

Validation
- Founder sees score + milestone modal; members don’t.
- Milestone modal appears once on crossing 10, dismisses on CTA/Done.
- Share sheet copies link and shows AGGIL note.

Risks & Mitigations
- Risk: repeated modal. Store dismissal flag client-side and server-side if possible.

Out of Scope
- Backend score computation (B10); notification toggle API (B04).
